[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm cache clean --force && npx vite build"]

[start]
cmd = "NODE_ENV=production node server/index.js"
